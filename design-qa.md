# Typer 古朴外观与机械连接验收 — 2026-09-22

## 2026-09-22 长卷与信邮

本次新增凯鲁亚克连续纸、文稿箱续写和账号间信邮。没有改变三款打字机的外壳、键盘比例及压纸机构；没有恢复作家书桌。独立验收使用 127.0.0.1:4175，不向用户的 4173 稿纸写入测试文本。

已取得的实际证据（`output/scroll-post-2026-09-22/`）：

- `Typer-scroll-report-120.json`、`Typer-scroll-report-10000.json`：Safari 实际保存并读回 120 / 10000 行，墨迹 seed 保留，屏幕只绘制 33 行。两种长度的活动行位置都与派生击打点相差 -0.2223 CSS 像素，在 1.5 像素容差内。
- `export-evidence.json`：Safari 实际下载 120 行长卷 ZIP，包含 2480×5956、2480×5956、2480×905 三张 PNG，编号连续。该证据为纹理接缝修正前的输出；修正后的视觉导出仍需重验。
- 实际打字机载入 120 行长卷后，用键盘回车到第 121 行，输入 `Still on the road.`，刷新后可访问文本仍包含完整这一行。直接发送 Unicode 文本的自动化输入没有落纸，因此不能据此声称通过了本轮系统中文 IME 验收。
- `http-mail-evidence.json`：独立服务上用两个测试账号经实际 HTTP 投递并拆信，状态 201、收信箱包含新信、选用的民国书简和归燕邮票保留，中文字迹与 seed 一致。这证明实际服务链路，不能替代前端封缄/拆信 UI 验收。
- `unit-tests.tap`：54 项通过。覆盖万行长卷窗口、坐标与完整分片；账号授权、未到时间禁止读信、服务重启后送达、幂等投递、每日限额和屏蔽。延迟场景使用可控时钟，无需等待实际 24 小时。
- `production-smoke.json`：构建后独立 Node 服务启动，页面与健康接口返回 200，注册后通过会话再次取得同一账号。
- `build.log` 与 `sites-tests.tap`：生产构建和受保护静态 Worker 测试通过。保护文件没有改动；这不是公网部署证据。

Safari 在收卷阅读及信邮 UI 验收过程中被 macOS 锁屏阻断；已请求用户解锁。待完成：修正纸纹后的实际视觉检查与下载、长卷阅读/装回续写、信邮的登录/封缄/拆信/回信，以及新界面的窄屏检查。现阶段不能标记这些视觉交互已通过。

运行说明与上线边界见 `docs/scroll-and-post-design-2026-09-22.md`。邮票资产来源见 `ASSET_NOTES.md`。


final result: passed

本结论覆盖古朴外观、压纸杆侧支架及对应机械检查，并保留整机尺寸、机型和撤下作家书桌的历史验收。完整视频逐帧复制不在本轮通过范围；未宣称所有设备、所有机械状态都已穷尽。凯鲁亚克模式和账号书信目前仅完成研究与方案，未包含在功能通过范围内。

## 最新变更：古朴外观

用户要求设计有打字机时代的气质，不能穿模。三款机型统一改为金属包边圆键与深色键面；事务机的现代方形格栅换为拱形铸壳浅浮雕，便携机使用铆接铭牌和带刻度的机械调节钮。经典机沿原轮廓增加细描线。五种涂装降低鲜艳度，使用确定性的细漆纹；纹理按零件自身形状裁切，不覆盖纸张或活动机构，不随击打随机闪动。

Safari 退纸检查额外发现压纸杆悬空：补齐左右铰接支架，与字车共同移动。支架位于可打印区域之外，连接滚筒轴座与横杆。原先已修复的回车杆连接保留，整机仍为 `min(120vw, 125dvh)`。

证据目录：`output/antique-design-2026-09-22/`。

- `before-main-safari.png` 为改动前实际页面；`classic-qa.png`、`portable-qa.png`、`editor-qa.png` 为改动后三款同配色、同中文状态的独立检查画面。
- `portable-strike.png`、`ivory-return.png`、`ivory-ejected.png` 分别核对击打、回车和退纸，最终压纸杆支架包含在这些图片中。
- `typebar-occlusion-report.json`：三款机型 × 五种配色 × 16 字杆 × 33 姿态，共 7920 帧，通过；290,370,960 个受保护像素比较无异常。该扫描完成后只增加字车支架，未再改动机身和字杆绘制。
- `carriage-occlusion-report.json`：增加支架后重新检查 150 组完整字迹行、165 次回车杆连通性、50 次整套字车连通性及 20 个中文字形范围，全部通过。纸口裁切在退纸过程中保持不变。
- 最终 `npm run build` 通过；现有 50 项测试通过，见 `unit-tests.tap`。实际 Safari 为 1235 × 768 窗口。
- `final-main-safari.png`：正式页面刷新后停留在用户选择的经典开放式、黑漆黄铜。用户工作期间写到第 7 行，刷新前后可访问文本逐字比较相同。没有为检查写入或清空用户稿件。

本轮新增方向的研究见 `docs/scroll-and-post-design-2026-09-22.md`；没有把本地原型描述为已上线的账号投递服务。修改前源文件与上一版验收记录保存于证据目录 `before/`。

## 历史变更：撤下作家书桌

用户明确取消作家书桌。已删除该模式的渲染分支、场景配置模块及专属样式，移除工作台的写作模式与场景切换；启动时清除旧模式偏好，直接进入机械打字机。

实际 Safari 原先停留在夜灯木桌、稿件第 6 行。更新后自动进入机械打字机，原有文字与第 6 行状态仍在，沿用用户选择的编辑室事务机和骨瓷镍银。打开工作台后直接看到机型与配色，未再出现作家书桌入口。证据：`output/retire-desk-2026-09-22/workbench-safari.png`，1235 × 768。

`npm run build` 与现有 50 项测试通过；保留稿纸箱、文稿箱和 PNG 导出入口。本次没有改动机械几何与墨迹算法，因此未重复上一轮机械像素扫描。修改前源码与上一版验收记录存于该证据目录的 `before/`。

## 来源与设计目标

- 尺寸依据：`output/text-clarity-2026-09-22/before/src/referenceMachine.css` 的原始 `min(120vw, 125dvh)`。用户明确否定了为文字清晰度而放大整机。
- 机型依据：当前经典开放式机器及用户要求“变种有外形设计，不能只换色”。新增便携式与事务机是这一轮的实体外观设计，并非对新外部照片的复制。
- 机械问题来源：`output/machine-models-2026-09-22/user-reported-lever.png`，374 × 244，握柄与回车杆、杆件与支点均不连续。
- Safari 实际写作页：`http://127.0.0.1:4173/`；独立检查入口 `/qa/variants.html` 不进入生产构建。

## 发现、修复与比较历史

1. [P1，已修复] 整机被放大约 52%。恢复原始相机比例，纸张、字车和外壳按同一比例缩放；墨迹模型和 PNG 尺度未变。
2. [P1，已修复] 五个选项只有配色差异。把机型和配色分开：经典机保留圆键和外露字杆；便携机有包覆圆壳、方圆键和提手；事务机有阶梯式铸壳、方键和独立机械舱。三款共用字车及落字坐标，但机壳轮廓、键尺寸和键盘排布有独立定义。
3. [P2，已修复] 便携机的中央开口最初透出房间背景。补齐机壳内的暗色机械舱，保留真正开口内可见的字杆。最终同色截图与穿模检查均已重做。
4. [P1，已修复] 原回车杆是悬空握柄加一条独立直杆。重建为连续弯杆：金属尾端进入握柄 27 个逻辑像素，根部通过轴座固定在滚筒左端；整组绕同一支点旋转，并随字车水平移动。主页面刷新后再次确认新杆生效。
5. 首次按键新测试把圆键的矩形包围盒重叠误报为相撞；已改为真实圆角轮廓的间距检查，并包含静止底座与按键下压量。没有缩减需要覆盖的按键或运动状态。

## 同视口证据

本段文件均位于 `output/machine-models-2026-09-22/`。Safari 截图为 1235 × 768，页面内容约 1235 × 723；QA 工具条占据顶部，机械接口放大只用于独立检查页，正式写作页保持恢复后的尺寸。

- `classic-qa.png`、`portable-qa.png`、`editor-qa.png`：相同视口、黑漆黄铜、同一中文稿纸状态；`models-same-finish.png` 用同一 764 × 404 裁区各缩放到 382 × 202，证明轮廓变化独立于颜色。
- `lever-comparison.png`：用户问题图与 Safari 静止、回车中段裁区放在同一输入中比对。裁区保持纵横比适配 374 × 244；不同原图尺度不作为坐标精度比较。
- `return-midstroke.png`：实际回车姿态，握柄和转轴之间保持连续。`portable-strike.png`：便携机落字瞬间，活动字杆从机械舱开口通过。
- `final-main-safari.png`：真实写作页已刷新，事务机与黑漆黄铜选择保留。刷新前后的可访问文本内容完全相同，用户三行稿纸未丢失。

## 五项视觉核验

- 字体：稿纸继续使用上一轮的共享机械墨迹模型；新键帽的字号与形状匹配，中文和英文纸面没有新增滤镜。
- 排布：整机按原比例展示；三种机型的键盘和外壳均完整。工作台把实体机型预览与小型配色样本分开。
- 颜色：同色对照已核对，配色可与机型独立组合，纸张颜色不受机身材质影响。
- 图像与部件：沿用当前独立活动零件的 Canvas 实现；无移动整机照片、无被切开的照片接缝。回车杆有完整轴座与杆件连接。
- 文案：保留 Typer 与原产品口号；新增机型名表达实际外形，未把单纯配色当作新机型。

## 验证结果

- Safari 像素检查：三款机型 × 五种配色 × 16 字杆 × 33 姿态，共 7920 帧，比较 290,370,960 个受保护像素，无检测异常。
- 150 组整行墨迹检查通过；20 个中文实际字形范围检查通过。
- 回车杆：五种配色 × 33 回车姿态，共 165 次实际渲染连通性检查，始终只有一个连通组件。
- 三款机型的每对按键分别覆盖静止、半压和全压组合，计入实体底座，间距检查通过。
- `npm run build` 通过；`node --test tests/*.test.mjs` 50 项通过。记录为 `unit-tests.tap`、`occlusion-report.json`。
- 实际 UI 已验证机型切换和刷新后选择持久化；当前稿纸保留。检查页已关闭，Safari 停留于正常写作页。

上轮墨迹验收原文保留于 `output/machine-models-2026-09-22/previous-design-qa.md`。本轮没有发布或推送。
