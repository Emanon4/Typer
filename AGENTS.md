# Prototype Instructions

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.

## Product Direction

- The user authorized publishing every current source change to the public `Emanon4/Typer` repository and deploying the frontend with GitHub Pages. Pages builds use `/Typer/` and the `pages` mode; verify dynamic paper, font and audio URLs under that base. The user requested all online features enabled. Pages connects to `https://typer-post.moji-pet.workers.dev`, backed by Cloudflare D1; verify real account registration and correspondence on the deployed API. Keep the complete local mail server and tests in the public source repository; never bundle the account database, passwords or session data into the Pages artifact.

- The formal product and repository name is `Typer`. Keep that name consistent in product copy, filenames, package metadata, local project naming, and remote repository naming. The product slogan is exactly `让你情不自禁地开始写作！`.
- This prototype is an experiential writing instrument, not a generic editor or landing page.
- Deeply simulate a real mechanical typewriter: fixed strike point, moving carriage, key depression, typebar strike, carriage return, paper advance, mechanical audio, and paper ejection.
- Chinese input is mandatory. Use the operating system IME and commit selected Chinese graphemes to paper without breaking composition events.
- Preserve typewriter constraints. Backspace moves the carriage but does not erase ink already printed on the sheet.
- Keep the visual direction original while matching the supplied video's warm, dark, tactile composition. Do not reproduce the video's brand or watermark.
- Use a photorealistic paper scan with visible fiber and small manufacturing variation for both the live sheet and exported output.
- Keep paper feed and printed-line movement on one shared coordinate system so carriage return never separates the sheet from the platen.
- Align every raster key legend to the measured center of its photographed keycap; do not approximate rows with a uniform gap.
- Finished drafts must export as a high-resolution PNG that preserves the same paper and ink rendering seen on screen.
- Keep the original video's paper bail rod and rubber rollers as a visible foreground hardware layer aligned to the moving carriage.
- The paper box must preserve both the source-video paper and the high-fiber paper, add period-appropriate letter, grid, and ruled stock, persist selection, and use the selected stock in live writing, review, and export.
- The source-video stock is the default. At line zero only the same shallow paper strip seen in the source may sit above the platen; carriage return must physically lift the entire sheet while the strike point and paper bail stay fixed.
- The user retired the writer's-desk experience on 2026-09-22. Keep only the mechanical close-up, filling the viewport without side bars or a framed canvas. Preserve the machine's proportions while the environment fills the available space. Do not restore the desk mode or scene selector; the sheet/scroll paper selector is allowed; old stored desk preferences must open the mechanical typewriter.
- Live stock is an A4 sheet at the physical 210:297 ratio. Chinese copy should use ordinary tight text spacing, a slightly tighter line pitch, and enough vertical capacity for a full page instead of stopping after a short sample.
- The paper bail must remain visible without crossing the active printed line. Paper and machine occlusion must use one clean physical boundary; never duplicate a clipped machine raster in a way that creates seams or torn geometry.
- One physical typebar rises for each printed glyph. Different glyphs should visibly originate from different positions in the fan while converging on the same fixed strike point, as a real basket mechanism does.
- Ejected sheets can be deliberately stored in a persistent manuscript box and reopened later with their paper stock and ink rendering intact.
- The user subsequently requested variants of the current instrument. Keep exactly one visible typewriter at a time. The mechanical close-up offers black-and-brass, ivory-and-nickel, forest-and-brass, midnight-blue-and-steel, and wine-and-champagne finishes independently from physical machine models. The Night Lamp, Morning Study, and Rainy Midnight desk environments have been retired.
- Mechanical motion must be physically faithful. Chinese is produced by a hidden digital type-slug adapter at the real strike instant; the adapter changes only the printed glyph and never fakes or bypasses the visible linkage.
- The user's 2026-09-22 feedback supersedes the old photographed-exterior freeze for mechanical close-up: closely reproduce the supplied video's wide low black body, exposed silver fan, dark red ribbon spools, round brass-ringed keys, long spacebar, and brass platen knobs. Do not keep the old photograph's visibly different body or invent another model. Do not reproduce the source brand or watermark.
- Browser mechanics must use clean independently rendered parts and one shared state model. Never animate a clipped duplicate of the full machine photograph; if a part cannot move without exposing a raster seam, keep the photographic part fixed and express the motion with a seam-free DOM/CSS part.
- Build and validate one complete typewriter before adding more styles. The first gate is one key-to-typebar-to-paper-to-escapement causal chain with a shared paper-feed and strike coordinate system, with zero overlap between the active line and the paper bail.
- Match the reference video's carriage logic: paper and bail move horizontally as one carriage assembly; carriage return restores that assembly while advancing the A4 sheet by exactly one derived line pitch. The bail sits between the previous printed row and the active strike row at every line index.
- Use `飞书20260727-181415.mp4` and its inspected frames as the mechanical close-up visual source of truth. Render the static body, moving carriage and live paper independently so full-screen composition does not stretch the instrument or reintroduce photograph seams.
- The user rejected the generated whole-machine approximation and authorized the independent-part rebuild. Measure the video frame directly for the close-up; every moving key and typebar must be an actual separately rendered part. Do not restore `reference-machine-body-v1.png` or `reference-carriage-v1.png` as the active close-up machine.
- Zero visible interpenetration is a hard acceptance gate for every finish. Keep the paper-mouth mask in place during ejection; draw moving typebars behind the opaque basket cover, ribbon spools and strike guide. Include shadows and glyph jitter in hardware clearance bounds. Test all typebars through their full travel and inspect browser renders; static thumbnails or a successful build alone are insufficient.
- Printed text must remain sharp at native size and browser zoom. Use scalable single-impression text without blur, offset double strikes or destructive grain; live writing and PNG export must share the same typography. The complete active line must be unobstructed at rest: place fixed guide hardware below its ink bounds, and lower the ribbon after the physical strike. Validate the full printed row, not just the central impact point.
- The user rejected perfectly clean digital type. Preserve a readable mechanical ink impression with restrained pressure variation, small irregular paper-fiber voids and slightly uneven ink edges. Generate that wear from each saved glyph seed and use the identical normalized impression in the live sheet and exported PNG. Do not regain texture by blurring or adding displaced duplicate glyphs.
- The user explicitly selected 京华老宋体 for the Chinese ink printed by the machine, clarified as the paper's text rather than interface labels. Use the bundled unmodified `KingHwaOldSong-2.002.ttf` at its regular 400 weight for Chinese glyphs and full-width punctuation, consistently in live paper, manuscript/letter previews and PNG export. Await the font before Canvas export; keep mechanical geometry and the interface typography unchanged.
- Keep the mechanical close-up at its original `min(120vw, 125dvh)` rig size. Text readability or ink changes must not enlarge the entire machine. The user rejected the enlarged `min(150vw, 190dvh)` composition.
- Machine variants must have visibly different exterior silhouettes and physical design, not only paint changes. Treat machine model and finish as independent choices; preserve one visible instrument, shared paper/strike mechanics, and obstruction checks for each model.
- Mechanical handles must visibly attach to their rods and mounting hardware. The return-lever grip, continuous bent arm, and carriage-mounted pivot form one connected assembly; animate the entire lever about that pivot, never a separate floating grip or disconnected segment. Validate actual raster connectivity throughout return travel.
- The user wants a restrained antique typewriter-era design. Prefer rolled metal key rings, dark key faces, cast reliefs and small engraved maker plates. Avoid modern square computer-style keys and appliance-like ventilation grilles. Material wear must be repeatable and clipped to each solid part; it must never extend onto the paper or move independently from its part. Preserve the existing machine scale and physical clearance rules.
- The user authorized Kerouac mode on 2026-09-22. Continuous paper has no page-end interruption or automatic ejection; keep the existing fixed strike point and machine scale. Store all lines in IndexedDB while rendering a bounded window. 收卷 archives a complete snapshot that can be reopened and continued. Export long rolls as ordered 2480-pixel-wide PNG segments in one ZIP; never silently truncate them. This does not restore the retired writer's desk.
- The user authorized personal correspondence with stationery, stamps and actual account-to-account delivery. The local implementation uses handle@typer addresses, authenticated server sessions and SQLite persistence. Defaults are 3 successfully posted letters per natural day and a recipient minimum wait of 24 hours; both are configurable. Draft writing is unlimited. A sender may choose a longer postal delay, but cannot bypass the recipient's minimum. Verify server-side delivery timing, immutable letter snapshots, quota enforcement, retry idempotency and account isolation. Letter drafts stay scoped to their account and must not enter the shared manuscript box. This local implementation is not a public deployment; Sites remains the protected static handoff. See `docs/scroll-and-post-design-2026-09-22.md`.
- The online post office uses the same shared service as local SQLite, with an asynchronous D1 adapter. Cross-site Pages authentication uses a tab-scoped bearer session so Safari does not depend on third-party cookies. Store only token hashes in D1. Recipient delay, quota and retry idempotency must remain atomic on the server under concurrent requests.
- The user requested a dedicated stamp album and richly detailed antique stamps inspired by cultures across the world. The initial collection contains twelve original Typer designs, including ten new civilization and botanical illustrations. Preserve local favorites and the preferred stamp for the next letter. Describe these as original artworks, never as authentic historical postal issues or fabricated rarities.
- Every visible typewriter key must be clickable with a mouse and physically depress. Use the exact same key geometry and printing queue as physical-keyboard input, including Shift and space. Keep IME composition intact and retain all existing collision constraints.
- The paper box includes six illustrated postcards alongside the five original paper stocks. A postcard has a landscape 148:105 ratio, a separate textured writing face and a reversible illustrated front. Export both faces as high-resolution PNGs in one ZIP. Switching paper shape must preserve the previous manuscript; account letters remain in their private drafts. Live, review, delivery and export must use the same saved postcard format and ink seeds.


<claude-mem-context>
# Memory Context

# [Typer] recent context, 2026-09-09 9:41am GMT+8

No previous sessions found.
</claude-mem-context>
